{
  "name": "@gloomhaven-enhanced/api",
  "version": "1.3.0",
  "description": "APIs for the Gloomhaven Enhanced TTS mod.",
  "main": "./build/index.js",
  "typings": "./build/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "build:lua": "tstl",
    "build:api": "typedoc src/index.ts",
    "build:api-watch": "typedoc --watch src/index.ts"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typedoc": "^0.22.13",
    "typescript": "^4.6.0",
    "typescript-to-lua": "^1.4.2"
  }
}
